
<% if @ore_nodes.size > 0 %>
	$(".ore").remove()

	<% @ore_nodes.each do |ore| %>
		{
			var node = "<%= j(render('/ore_nodes/ore', :@ore => ore)) %>";
			
			$("#map").append(node)
		}
	<% end %>

	
	$("[rel=tooltip]").tooltip()
<% end %>
