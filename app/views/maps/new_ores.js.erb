
<% if @new_ores.size > 0 %>
	<% @new_ores.each do |ore| %>
		{
			var node = "<%= j(render('/ore_nodes/ore', :@ore => ore)) %>";
			
			$("#map").append(node)
		}
	<% end %>

	
	$("[rel=tooltip]").tooltip()
<% end %>
